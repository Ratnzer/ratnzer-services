name: Unified App Build & Release

permissions:
  contents: write
  pull-requests: write

on:
  push:
    branches: [ "main" ]
    tags:
      - '*.*.*'
      - '*'
  workflow_dispatch:
    inputs:
      version:
        description: 'رقم الإصدار اليدوي (اختياري)'
        required: false
        type: string

jobs:
  build-and-release:
    name: Build & Release
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: تحديد رقم الإصدار ونوع العملية
        id: vars
        run: |
          IS_TAG_EVENT=false
          if [[ "${{ github.ref }}" == refs/tags/* ]] || [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ github.event.inputs.version }}" != "" ]]; then
            IS_TAG_EVENT=true
          fi
          echo "is_tag_event=$IS_TAG_EVENT" >> $GITHUB_OUTPUT
          
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ github.event.inputs.version }}" != "" ]]; then
            VERSION="${{ github.event.inputs.version }}"
          elif [[ "${{ github.ref }}" == refs/tags/* ]]; then
            TAG_NAME=${GITHUB_REF#refs/tags/}
            VERSION=${TAG_NAME#v}
          else
            LATEST_RELEASE=$(gh release list --limit 1 --json tagName --jq '.[0].tagName' 2>/dev/null || echo "")
            if [ -n "$LATEST_RELEASE" ]; then
              VERSION=${LATEST_RELEASE#v}
            else
              VERSION=$(node -p "require('./package.json').version")
            fi
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 الإصدار المعتمد: $VERSION"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Java (JDK 17)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Validate Firebase build variables
        run: |
          test -n "${{ secrets.VITE_FIREBASE_API_KEY }}"
          test -n "${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}"
          test -n "${{ secrets.VITE_FIREBASE_PROJECT_ID }}"
          test -n "${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}"
          test -n "${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}"
          test -n "${{ secrets.VITE_FIREBASE_APP_ID }}"

      - name: تحديث رقم الإصدار في الملفات
        env:
          APP_VERSION: ${{ steps.vars.outputs.version }}
        run: npm run update-version

      - name: Build Web Assets
        env:
          VITE_API_URL: https://ratnzer-services-bb0a0cce4837.herokuapp.com/api
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
        run: npm run build

      - name: ضغط ملفات الويب (Web Build)
        run: |
          zip -r web-build-v${{ steps.vars.outputs.version }}.zip dist/

      - name: Initialize Android Platform
        run: |
          rm -rf android
          npx cap add android

      - name: تحديث رقم إصدار Android
        env:
          APP_VERSION: ${{ steps.vars.outputs.version }}
        run: npm run update-android-version

      - name: Add google-services.json (base64)
        run: |
          mkdir -p android/app
          printf "%s" "${{ secrets.GOOGLE_SERVICES_JSON_B64 }}" | base64 --decode > android/app/google-services.json
          test -s android/app/google-services.json

      - name: Setup Facebook Authentication Config
        run: |
          echo "إعداد ملف strings.xml مع Facebook App ID..."
          STRINGS_FILE="android/app/src/main/res/values/strings.xml"
          mkdir -p android/app/src/main/res/values
          echo '<?xml version="1.0" encoding="utf-8"?>' > "$STRINGS_FILE"
          echo '<resources>' >> "$STRINGS_FILE"
          echo '    <string name="app_name">خدمات راتنزر</string>' >> "$STRINGS_FILE"
          echo '    <string name="title_activity_main">خدمات راتنزر</string>' >> "$STRINGS_FILE"
          echo '    <string name="package_name">com.ratnzer.app</string>' >> "$STRINGS_FILE"
          echo '    <string name="custom_url_scheme">com.ratnzer.app</string>' >> "$STRINGS_FILE"
          echo '    <string name="facebook_app_id">2994111874110231</string>' >> "$STRINGS_FILE"
          echo '    <string name="fb_login_protocol_scheme">fb2994111874110231</string>' >> "$STRINGS_FILE"
          echo '    <string name="facebook_client_token">c55884326412a3468d50b1952f047bbd</string>' >> "$STRINGS_FILE"
          echo '</resources>' >> "$STRINGS_FILE"
          echo "تم إنشاء ملف strings.xml بنجاح"
          cat "$STRINGS_FILE"

      - name: Verify AndroidManifest.xml Facebook Config
        run: |
          echo "التحقق من إعدادات Facebook في AndroidManifest.xml..."
          MANIFEST_FILE="android/app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST_FILE" ]; then
            echo "ملف AndroidManifest.xml موجود"
            if grep -q "com.facebook.sdk.ApplicationId" "$MANIFEST_FILE"; then
              echo "Facebook ApplicationId موجود في AndroidManifest"
            else
              echo "تحذير: Facebook ApplicationId غير موجود في AndroidManifest"
              echo "سيتم الاعتماد على Capacitor لإضافته تلقائياً..."
            fi
            echo "--- بداية AndroidManifest.xml ---"
            head -30 "$MANIFEST_FILE"
            echo "--- نهاية المقتطف ---"
          else
            echo "خطأ: ملف AndroidManifest.xml غير موجود!"
            exit 1
          fi

      - name: Install Capacitor Assets Tool
        run: npm i -D @capacitor/assets --legacy-peer-deps

      - name: Generate Android assets from resources
        run: npx @capacitor/assets generate --android

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Final Verification of Required Files
        run: |
          echo "التحقق النهائي من جميع الملفات المطلوبة للمصادقة..."
          ERRORS=0
          if [ -f "android/app/google-services.json" ]; then
            echo "google-services.json موجود"
            if [ -s "android/app/google-services.json" ]; then
              echo "google-services.json يحتوي على بيانات"
            else
              echo "google-services.json فارغ!"
              ERRORS=$((ERRORS + 1))
            fi
          else
            echo "google-services.json غير موجود!"
            ERRORS=$((ERRORS + 1))
          fi
          if [ -f "android/app/src/main/res/values/strings.xml" ]; then
            echo "strings.xml موجود"
            if grep -q "facebook_app_id" "android/app/src/main/res/values/strings.xml"; then
              echo "Facebook App ID موجود في strings.xml"
            else
              echo "Facebook App ID غير موجود في strings.xml!"
              ERRORS=$((ERRORS + 1))
            fi
          else
            echo "strings.xml غير موجود!"
            ERRORS=$((ERRORS + 1))
          fi
          if grep -q "FirebaseAuthentication" "capacitor.config.ts"; then
            echo "إعدادات FirebaseAuthentication موجودة في capacitor.config.ts"
          else
            echo "تحذير: إعدادات FirebaseAuthentication غير موجودة في capacitor.config.ts"
            echo "يُفضل إضافتها لضمان عمل المصادقة بشكل صحيح"
          fi
          if [ -f "android/app/build.gradle" ]; then
            echo "build.gradle موجود"
          else
            echo "build.gradle غير موجود!"
            ERRORS=$((ERRORS + 1))
          fi
          echo ""
          if [ $ERRORS -eq 0 ]; then
            echo "جميع الملفات المطلوبة موجودة وصحيحة!"
          else
            echo "هناك $ERRORS ملفات ناقصة أو بها مشاكل!"
            echo "سيتم متابعة البناء، لكن قد تحدث مشاكل في تسجيل الدخول..."
          fi

      - name: Build APK & AAB (Always Release)
        run: |
          cd android
          chmod +x gradlew
          echo "بناء ملف APK (للتثبيت المباشر)..."
          ./gradlew assembleRelease
          echo "بناء ملف AAB (لمتجر جوجل بلاي)..."
          ./gradlew bundleRelease

      - name: توقيع ملف APK
        uses: r0adkll/sign-android-release@v1
        id: sign_apk
        with:
          releaseDirectory: android/app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          alias: ${{ secrets.ANDROID_KEY_ALIAS }}
          keyStorePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: توقيع ملف AAB (لمتجر بلاي)
        uses: r0adkll/sign-android-release@v1
        id: sign_aab
        with:
          releaseDirectory: android/app/build/outputs/bundle/release
          signingKeyBase64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          alias: ${{ secrets.ANDROID_KEY_ALIAS }}
          keyStorePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: إنشاء Release ورفع الملفات
        if: steps.vars.outputs.is_tag_event == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.vars.outputs.version }}
          name: الإصدار ${{ steps.vars.outputs.version }}
          files: |
            ${{ steps.sign_apk.outputs.signedReleaseFile }}
            ${{ steps.sign_aab.outputs.signedReleaseFile }}
            web-build-v${{ steps.vars.outputs.version }}.zip
          body: |
            ## الإصدار ${{ steps.vars.outputs.version }}
            تم بناء وتوقيع الملفات تلقائياً مع دعم كامل لتسجيل الدخول عبر Google و Facebook.
            
            ### الملفات المرفقة:
            1. APK: للتثبيت المباشر على الهواتف.
            2. AAB: للرفع على متجر جوجل بلاي (Google Play Store).
            3. Web Build: ملفات الويب جاهزة للنشر على أي استضافة.
            
            ### تحسينات هذا الإصدار:
            - دعم كامل لتسجيل الدخول عبر Google
            - دعم كامل لتسجيل الدخول عبر Facebook
            - تحسينات في عملية البناء والتحقق من الملفات
          draft: false
          prerelease: false

      - name: رفع الملفات كـ Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Ratnzer-App-v${{ steps.vars.outputs.version }}
          path: |
            ${{ steps.sign_apk.outputs.signedReleaseFile }}
            ${{ steps.sign_aab.outputs.signedReleaseFile }}
            web-build-v${{ steps.vars.outputs.version }}.zip
          retention-days: 14
