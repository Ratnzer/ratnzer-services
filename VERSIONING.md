# 📦 نظام إدارة الإصدارات (Versioning System)

تم إعداد نظام إدارة إصدارات تلقائي لتطبيق راتنزر يعتمد على **GitHub Releases**.

## 🎯 كيف يعمل النظام؟

### 1️⃣ البناء التلقائي (Auto Build)
عند كل `push` إلى الفرع `main`، يتم:
- جلب أحدث رقم إصدار من **GitHub Releases**
- إذا لم يوجد Release، يستخدم الإصدار من `package.json`
- تحديث جميع الملفات برقم الإصدار
- بناء APK تلقائياً

### 2️⃣ إنشاء إصدار جديد (Create Release)
لإنشاء إصدار جديد، لديك خيارين:

#### الطريقة الأولى: باستخدام Git Tags
```bash
# تحديث رقم الإصدار محلياً
npm version patch  # أو minor أو major

# رفع التغييرات والـ tag
git push && git push --tags
```

#### الطريقة الثانية: يدوياً من GitHub Actions
1. اذهب إلى تبويب **Actions** في المستودع
2. اختر **Create Release and Build APK**
3. اضغط **Run workflow**
4. أدخل رقم الإصدار (مثال: `3.3.7`)
5. سيتم إنشاء Release وبناء APK تلقائياً

## 📁 الملفات المتأثرة بالإصدار

عند تحديث الإصدار، يتم تحديث:
- ✅ `package.json` - رقم الإصدار الأساسي
- ✅ `capacitor.config.ts` - إصدار Capacitor
- ✅ `android/app/build.gradle` - `versionCode` و `versionName`
- ✅ `src/version.json` - ملف يحتوي على معلومات الإصدار للاستخدام في التطبيق

## 🔧 السكريبتات المتاحة

```bash
# تحديث رقم الإصدار في جميع الملفات
npm run update-version

# تحديث رقم الإصدار في Android فقط
npm run update-android-version
```

## 📱 استخدام رقم الإصدار في التطبيق

يمكنك عرض رقم الإصدار في التطبيق:

```typescript
import versionInfo from './version.json';

console.log(`الإصدار: ${versionInfo.version}`);
console.log(`تاريخ البناء: ${versionInfo.buildDate}`);
```

## 🚀 مثال عملي

### إصدار جديد برقم 3.4.0

```bash
# 1. تحديث الإصدار محلياً
npm version minor  # سيصبح 3.4.0

# 2. رفع التغييرات
git push && git push --tags

# 3. سيتم تلقائياً:
#    - إنشاء Release على GitHub
#    - بناء APK بالإصدار 3.4.0
#    - رفع APK إلى Release
```

## 📊 تنسيق رقم الإصدار

نستخدم **Semantic Versioning** (SemVer):
- **MAJOR.MINOR.PATCH** (مثال: `3.3.6`)
- **MAJOR**: تغييرات كبيرة غير متوافقة
- **MINOR**: إضافة ميزات جديدة متوافقة
- **PATCH**: إصلاح أخطاء

### تحويل إلى versionCode للأندرويد
- `3.3.6` → `30306`
- `3.4.0` → `30400`
- `4.0.0` → `40000`

## ⚙️ GitHub Workflows

### 1. `build.yml` - البناء التلقائي
- يعمل عند كل push للـ main
- يستخدم أحدث Release version
- ينتج APK للتطوير (debug)

### 2. `release.yml` - إنشاء Release
- يعمل عند إنشاء tag أو يدوياً
- ينشئ Release على GitHub
- ينتج APK للإنتاج (release)
- يرفع APK إلى Release تلقائياً

## 🔐 ملاحظات مهمة

1. **التوقيع (Signing)**: حالياً APK غير موقّع. لإضافة التوقيع:
   - أضف keystore إلى GitHub Secrets
   - حدّث `release.yml` لتوقيع APK

2. **الإصدار الأول**: إذا لم يكن هناك Releases بعد:
   - سيستخدم النظام الإصدار من `package.json`
   - أنشئ أول Release يدوياً أو باستخدام tag

3. **متغير البيئة**: يمكنك تمرير `APP_VERSION` يدوياً:
   ```bash
   APP_VERSION=3.5.0 npm run update-version
   ```

## 📞 الدعم

إذا واجهت أي مشكلة، تحقق من:
- سجلات GitHub Actions
- ملف `package.json` يحتوي على رقم إصدار صحيح
- وجود Releases في المستودع
